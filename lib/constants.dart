import 'package:flutter/material.dart';

const kPrimaryColor = Color(0xff100B20);

const kNavigationDuration = Duration(milliseconds: 200);

const kFeaturedBooksBox = 'featured_books';
const kNewestBooksBox = 'newest_books';
